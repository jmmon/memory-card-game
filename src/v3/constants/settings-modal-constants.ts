const COLUMN_GAP = "gap-0.5 md:gap-1" as const;
const REQUIRES_RESTART = "Requires restart to take effect." as const;

export const settingsModalConstants = { COLUMN_GAP, REQUIRES_RESTART } as const;
