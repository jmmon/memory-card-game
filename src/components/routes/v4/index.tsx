import { component$ } from "@builder.io/qwik";

/* 
* Play with not using a grid?
* use absolute positioning to place and shuffle the cards
* that allows easy transitions
*
* */
export default component$(() => {
  return (
    <div class="flex flex-col w-full h-screen items-center">
      <h1>v3 Route</h1>
{/* game */}
    </div>
  );
});
